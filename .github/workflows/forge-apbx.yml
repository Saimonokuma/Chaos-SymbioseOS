# ═══════════════════════════════════════════════════════════════════════════════
# CHAOS-SYMBIOSE OS — CI/CD Forge Pipeline
# File: .github/workflows/forge-apbx.yml
# Ref:  Interactive_Plan.md §II (CI/CD FORGE & PIPELINE ARCHITECTURE)
#
# 6-job pipeline: 4 parallel build jobs → assemble → seal
# Runners: Self-hosted Windows (WDK) + GitHub-hosted Ubuntu
# Triggers: push to main/release/*, PR to main, manual dispatch
# ═══════════════════════════════════════════════════════════════════════════════

name: "Forge APBX — SymbioseOS Build Pipeline"

on:
  push:
    branches: [main, "release/*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

# ─────────────────────────────────────────────────────────────────────────────
# ENV: Shared constants across all jobs
# ─────────────────────────────────────────────────────────────────────────────
env:
  STAGING_DIR: "04_APBX_Transmigration/playbook"
  APBX_PASSWORD: "malte"

jobs:
  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 1: build-kmdf-driver (CI-001)
  # §II·3 Job 1 — WDK/MSBuild: symbiose_bridge.sys + SymbioseNull.sys
  # Runner: GitHub-hosted Windows (WDK installed via setup action)
  # ═══════════════════════════════════════════════════════════════════════════
  build-kmdf-driver:
    name: "CI-001 · Build KMDF Driver + WDM Filter"
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup WDK
        uses: egor-tensin/setup-wdk@v1
        with:
          version: latest

      # Build the Ring-0 KMDF hypervisor driver (§I·2, §XV)
      - name: Build symbiose_bridge.sys (KMDF)
        run: |
          msbuild 02_Symbiose_Bridge/symbiose_bridge.vcxproj `
            /p:Configuration=Release `
            /p:Platform=x64 `
            /p:DriverType=KMDF `
            /p:SignMode=TestSign
        shell: powershell

      # Build the WDM NVMe isolation filter (BRIDGE-009/011)
      - name: Build SymbioseNull.sys (WDM filter)
        run: |
          msbuild 02_Symbiose_Bridge/SymbioseNull/SymbioseNull.vcxproj `
            /p:Configuration=Release `
            /p:Platform=x64 `
            /p:SignMode=TestSign
        shell: powershell

      - name: Verify driver outputs
        run: |
          $files = @(
            "02_Symbiose_Bridge/x64/Release/symbiose_bridge.sys",
            "02_Symbiose_Bridge/SymbioseNull/x64/Release/SymbioseNull.sys"
          )
          foreach ($f in $files) {
            if (-not (Test-Path $f)) { throw "MISSING: $f" }
            Write-Host "[OK] $f ($(( Get-Item $f).Length) bytes)"
          }
        shell: powershell

      - uses: actions/upload-artifact@v4
        with:
          name: drivers
          path: |
            02_Symbiose_Bridge/x64/Release/symbiose_bridge.sys
            02_Symbiose_Bridge/x64/Release/symbiose_bridge.cat
            02_Symbiose_Bridge/SymbioseNull/x64/Release/SymbioseNull.sys
            02_Symbiose_Bridge/inf/SymbioseNull.inf
          retention-days: 30

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 2: build-win32-binaries (CI-002)
  # §II·3 Job 2 — MinGW-w64: ChaosLoader.exe + symbiose_ircd.exe
  # Runner: GitHub-hosted Windows (MinGW installed via setup action)
  # Build flags (§II·5):
  #   ChaosLoader:    -O3 -flto -static-libgcc -municode
  #   symbiose_ircd:  -O3 -flto -static-libgcc -lws2_32
  # ═══════════════════════════════════════════════════════════════════════════
  build-win32-binaries:
    name: "CI-002 · Build Win32 Usermode Binaries"
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup MinGW-w64
        uses: egor-tensin/setup-mingw@v2
        with:
          platform: x64

      # Build ChaosLoader.exe — Ring-3 VM launcher (§III, §V)
      - name: Build ChaosLoader.exe
        run: |
          mingw32-make -C 03_HiveMind_Orchestrator/ChaosLoader `
            -f Makefile.win64 release
        shell: powershell

      # Build symbiose_ircd.exe — Host-side IRC Neural Bus (§VII)
      - name: Build symbiose_ircd.exe
        run: |
          mingw32-make -C 03_HiveMind_Orchestrator/IRCd_Neural_Bus `
            -f Makefile.win64 release
        shell: powershell

      - name: Verify usermode outputs
        run: |
          $files = @(
            "03_HiveMind_Orchestrator/ChaosLoader/build/ChaosLoader.exe",
            "03_HiveMind_Orchestrator/IRCd_Neural_Bus/build/symbiose_ircd.exe"
          )
          foreach ($f in $files) {
            if (-not (Test-Path $f)) { throw "MISSING: $f" }
            Write-Host "[OK] $f ($(( Get-Item $f).Length) bytes)"
          }
        shell: powershell

      - uses: actions/upload-artifact@v4
        with:
          name: usermode
          path: |
            03_HiveMind_Orchestrator/ChaosLoader/build/ChaosLoader.exe
            03_HiveMind_Orchestrator/IRCd_Neural_Bus/build/symbiose_ircd.exe
          retention-days: 30

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 3: build-terminal-ui (CI-005)
  # §II·3 Job 3 — Tauri 2.0 (Rust + React): SymbioseTerminal.exe
  # Runner: GitHub-hosted Windows (Rust + Node.js installed via setup actions)
  # ═══════════════════════════════════════════════════════════════════════════
  build-terminal-ui:
    name: "CI-005 · Build SymbioseTerminal.exe (Tauri 2.0)"
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup Rust (MSVC target)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: "x86_64-pc-windows-msvc"

      # Build Tauri 2.0 frontend + Rust backend (§XVIII)
      - name: Build Tauri frontend
        run: |
          cd 06_Terminal_UI
          npm ci
          npm run tauri build -- --target x86_64-pc-windows-msvc
        shell: powershell

      - name: Verify Terminal UI output
        run: |
          $exe = "06_Terminal_UI/src-tauri/target/x86_64-pc-windows-msvc/release/SymbioseTerminal.exe"
          if (-not (Test-Path $exe)) { throw "MISSING: $exe" }
          Write-Host "[OK] $exe ($(( Get-Item $exe).Length) bytes)"
        shell: powershell

      - uses: actions/upload-artifact@v4
        with:
          name: terminal-ui
          path: 06_Terminal_UI/src-tauri/target/x86_64-pc-windows-msvc/release/SymbioseTerminal.exe
          retention-days: 30

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 4: build-linux (CI-003)
  # §II·3 Job 4 — Ubuntu/Debian: x86_64 kernel + hive_mind + initrd
  # Runner: GitHub-hosted ubuntu-latest in debian:bookworm container
  #
  # Builds:
  #   1. x86_64 BZIMAGE from 01_Chaos_Kernel/ source (NOT 2004 original)
  #   2. hive_mind static binary (musl, PID 1 — §XVII·3)
  #   3. llama-server static binary (musl, from llama.cpp submodule)
  #   4. symbiose_ircd static binary (musl, guest-side)
  #   5. initrd.img (cpio+gzip newc archive — §XVII·2)
  # ═══════════════════════════════════════════════════════════════════════════
  build-linux:
    name: "CI-003 · Build Linux Kernel, hive_mind, and initrd"
    runs-on: ubuntu-latest
    container: debian:bookworm
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          apt-get update && apt-get install -y \
            build-essential bc flex bison libssl-dev libelf-dev \
            cpio gzip wget file

      - name: Install musl cross-compiler
        run: |
          wget -q https://musl.cc/x86_64-linux-musl-cross.tgz
          tar -xf x86_64-linux-musl-cross.tgz -C /opt
          echo "/opt/x86_64-linux-musl-cross/bin" >> $GITHUB_PATH

      # ── Step 1: Rebuild x86_64 BZIMAGE from source ──
      # NOT the 2004 original — that's forensic reference (§I·3)
      # Config includes YeAH! TCP + CAKE QoS (KERNEL-009, §XIV·7)
      - name: Build x86_64 Chaos Kernel
        run: |
          cd 01_Chaos_Kernel
          make ARCH=x86_64 defconfig
          # Apply Symbiose-specific kernel config (§XIV·5)
          scripts/config --enable CONFIG_64BIT
          scripts/config --enable CONFIG_VIRTIO_PCI
          scripts/config --enable CONFIG_BLK_DEV_RAM
          scripts/config --enable CONFIG_SERIAL_8250
          scripts/config --enable CONFIG_TMPFS
          scripts/config --enable CONFIG_CGROUPS
          scripts/config --enable CONFIG_CHECKPOINT_RESTORE
          scripts/config --enable CONFIG_BPF_SYSCALL
          scripts/config --disable CONFIG_MODULES
          # YeAH! TCP + CAKE QoS (KERNEL-009)
          scripts/config --enable CONFIG_TCP_CONG_YEAH
          scripts/config --enable CONFIG_NET_SCH_CAKE
          scripts/config --enable CONFIG_NET_SCH_FQ_CODEL
          scripts/config --enable CONFIG_NET_CLS_FW
          scripts/config --set-str CONFIG_DEFAULT_TCP_CONG "yeah"
          # CAKE Ingress Shaping (IFB + mirred)
          scripts/config --enable CONFIG_NET_CLS_ACT
          scripts/config --enable CONFIG_NET_CLS_U32
          scripts/config --enable CONFIG_NET_ACT_MIRRED
          scripts/config --enable CONFIG_NET_SCH_INGRESS
          scripts/config --enable CONFIG_IFB
          scripts/config --enable CONFIG_BQL
          make ARCH=x86_64 -j$(nproc) bzImage
          mkdir -p ../build
          cp arch/x86/boot/bzImage ../build/BZIMAGE
          file ../build/BZIMAGE  # Must report: Linux kernel x86 boot executable

      # ── Step 2: Compile hive_mind PID 1 (static musl) ──
      # §XVII·3: Statically-linked C binary for x86_64-linux-musl
      - name: Compile hive_mind
        run: |
          x86_64-linux-musl-gcc -static -O2 \
            -o build/hive_mind \
            03_HiveMind_Orchestrator/ChaosLoader/src/hive_mind_init.c \
            03_HiveMind_Orchestrator/ChaosLoader/src/irc_client.c \
            03_HiveMind_Orchestrator/ChaosLoader/src/jumbo_payload.c
          file build/hive_mind  # Must report: ELF 64-bit LSB executable, statically linked

      # ── Step 3: Compile llama-server (static, llama.cpp submodule) ──
      - name: Compile llama-server
        run: |
          cd 03_HiveMind_Orchestrator/llama.cpp
          mkdir -p build && cd build
          cmake .. -DLLAMA_STATIC=ON \
                   -DCMAKE_C_COMPILER=x86_64-linux-musl-gcc \
                   -DCMAKE_CXX_COMPILER=x86_64-linux-musl-g++ \
                   -DCMAKE_BUILD_TYPE=Release
          make -j$(nproc) llama-server
          cp bin/llama-server ../../../build/llama-server
          file ../../../build/llama-server

      # ── Step 4: Compile symbiose_ircd (guest-side, static musl) ──
      - name: Compile symbiose_ircd (guest)
        run: |
          x86_64-linux-musl-gcc -static -O2 \
            -o build/symbiose_ircd \
            03_HiveMind_Orchestrator/IRCd_Neural_Bus/src/ircd_main.c
          file build/symbiose_ircd

      # ── Step 5: Rebuild initrd.img (cpio + gzip, §XVII·2) ──
      - name: Rebuild initrd.img
        run: |
          chmod +x rebuild_initrd.sh
          ./rebuild_initrd.sh "PLACEHOLDER_MODEL" "SafeTensors"
          file 04_APBX_Transmigration/playbook/Executables/initrd.img
          # Verify hive_mind is present in the archive
          gzip -dc 04_APBX_Transmigration/playbook/Executables/initrd.img \
            | cpio -t 2>/dev/null | grep -q "sbin/hive_mind"

      - uses: actions/upload-artifact@v4
        with:
          name: linux
          path: |
            build/BZIMAGE
            04_APBX_Transmigration/playbook/Executables/initrd.img
          retention-days: 30

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 5: assemble-apbx (CI-004a)
  # §II·3 Job 5 — Collect all 4 build artifacts → stage into APBX layout
  # Depends: ALL 4 build jobs must succeed
  # ═══════════════════════════════════════════════════════════════════════════
  assemble-apbx:
    name: "CI-004 · Assemble APBX Package"
    runs-on: windows-latest
    needs: [build-kmdf-driver, build-win32-binaries, build-terminal-ui, build-linux]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts/

      # Stage artifacts into APBX package layout (§IX·2)
      # NOTE: actions/download-artifact@v4 preserves nested build output paths.
      # We use Get-ChildItem -Recurse to locate files regardless of depth.
      - name: Stage APBX directory
        run: |
          $STAGING = "${{ env.STAGING_DIR }}"

          # Executables/
          New-Item -ItemType Directory -Force "$STAGING/Executables"
          Get-ChildItem -Path "artifacts/usermode" -Recurse -Filter "ChaosLoader.exe" |
            Copy-Item -Destination "$STAGING/Executables/"
          Get-ChildItem -Path "artifacts/usermode" -Recurse -Filter "symbiose_ircd.exe" |
            Copy-Item -Destination "$STAGING/Executables/"
          Get-ChildItem -Path "artifacts/terminal-ui" -Recurse -Filter "SymbioseTerminal.exe" |
            Copy-Item -Destination "$STAGING/Executables/"

          # Executables/Drivers/
          New-Item -ItemType Directory -Force "$STAGING/Executables/Drivers"
          Get-ChildItem -Path "artifacts/drivers" -Recurse -Filter "symbiose_bridge.sys" |
            Copy-Item -Destination "$STAGING/Executables/Drivers/"
          Get-ChildItem -Path "artifacts/drivers" -Recurse -Filter "symbiose_bridge.cat" |
            Copy-Item -Destination "$STAGING/Executables/Drivers/"
          Get-ChildItem -Path "artifacts/drivers" -Recurse -Filter "SymbioseNull.sys" |
            Copy-Item -Destination "$STAGING/Executables/Drivers/"
          Get-ChildItem -Path "artifacts/drivers" -Recurse -Filter "SymbioseNull.inf" |
            Copy-Item -Destination "$STAGING/Executables/Drivers/"

          # Executables/Kernel/
          New-Item -ItemType Directory -Force "$STAGING/Executables/Kernel"
          Get-ChildItem -Path "artifacts/linux" -Recurse -Filter "BZIMAGE" |
            Copy-Item -Destination "$STAGING/Executables/Kernel/"
          Get-ChildItem -Path "artifacts/linux" -Recurse -Filter "initrd.img" |
            Copy-Item -Destination "$STAGING/Executables/Kernel/"

          # Verify all 8 required files are present (§IX·2)
          $required = @(
            "$STAGING/Executables/ChaosLoader.exe",
            "$STAGING/Executables/symbiose_ircd.exe",
            "$STAGING/Executables/SymbioseTerminal.exe",
            "$STAGING/Executables/Drivers/symbiose_bridge.sys",
            "$STAGING/Executables/Drivers/SymbioseNull.sys",
            "$STAGING/Executables/Kernel/BZIMAGE",
            "$STAGING/Executables/Kernel/initrd.img",
            "$STAGING/playbook.conf"
          )
          foreach ($f in $required) {
            if (-not (Test-Path $f)) { throw "MISSING: $f" }
            Write-Host "[OK] $f"
          }
          Write-Host ""
          Write-Host "All 8 required files staged successfully."
        shell: powershell

      - uses: actions/upload-artifact@v4
        with:
          name: apbx-staged
          path: ${{ env.STAGING_DIR }}/
          retention-days: 30

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 6: seal-and-upload (CI-004b)
  # §II·3 Job 6 — 7z LZMA2+AES256 seal → GitHub Release on tag push
  # Password: "malte" (§II·4 playbook.conf)
  # ═══════════════════════════════════════════════════════════════════════════
  seal-and-upload:
    name: "CI-004 · Seal APBX + GitHub Release"
    runs-on: windows-latest
    needs: [assemble-apbx]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: apbx-staged
          path: ${{ env.STAGING_DIR }}/

      # Seal the playbook into a password-protected .apbx archive
      # Format: 7z LZMA2 ultra compression + AES-256 encryption + header encryption
      - name: Seal .apbx archive
        run: |
          cd 04_APBX_Transmigration
          7z a -t7z -p"${{ env.APBX_PASSWORD }}" -mhe=on -m0=lzma2 -mx=9 `
            ../Chaos-SymbioseOS.apbx `
            playbook/
          # Verify: archive opens with correct password and contains playbook.conf
          7z l -p"${{ env.APBX_PASSWORD }}" ../Chaos-SymbioseOS.apbx | Select-String "playbook.conf"
          Write-Host "[OK] Chaos-SymbioseOS.apbx sealed ($(( Get-Item ../Chaos-SymbioseOS.apbx).Length) bytes)"
        shell: powershell

      - name: Upload .apbx artifact
        uses: actions/upload-artifact@v4
        with:
          name: Chaos-SymbioseOS-apbx
          path: Chaos-SymbioseOS.apbx
          retention-days: 90

      # Create GitHub Release only on tag push (e.g., v3.0.0)
      - name: Create GitHub Release (on tag push only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: Chaos-SymbioseOS.apbx
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
